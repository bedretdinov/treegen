from setuptools import setup, find_packages

setup(
   name='treegen',
   version='1.0',
   description='Generate if else tree from tree sklearn model',
   author='Nader Bedretdinov',
   author_email='php-job@mail.ru',
   packages=find_packages()
)